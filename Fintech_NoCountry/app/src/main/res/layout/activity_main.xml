<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical">
    <!-- TODO ESTO SE PUEDE BORRAR -->
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Ingrese un id"
        android:textAlignment="center"
        android:textSize="15sp"
        android:id = "@+id/etId"
        android:layout_gravity="center"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btnEjecutar"
        android:text="Ejecutar Query"
        android:layout_gravity="center"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="La query a ingresar debe estar delimitada por '%',donde el primer segmento es la tabla a consultar, el segundo son las condiciones del WHERE y el tercero son las columnas a traer"
        android:textAlignment="center"
        android:textSize="15sp"
        android:id = "@+id/tvJson"
        android:layout_gravity="center"
        />

</LinearLayout>